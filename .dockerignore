# The service image builds with the repository root as its context (see
# docker/Dockerfile). Keep the context to what a build can use.
.git
.github
docs
**/dist
**/*.log
go.work
go.work.sum
smelt/snapshots
